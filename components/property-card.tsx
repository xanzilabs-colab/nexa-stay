import Image from "next/image";
import Link from "next/link";
import { Bath, BedDouble, Users } from "lucide-react";
import type { Property } from "@/types/property";
export function PropertyCard({ property }: { property: Property }) { const image = property.images.find(image => image.is_primary) ?? property.images[0]; const period = property.price_period === "per_month" ? "/ month" : "/ night"; const price = property.price_period === "price_on_request" ? "Price on request" : <>R{property.price_per_night.toLocaleString()} <small>{period}</small></>; return <Link href={`/properties/${property.slug}`} className="property-card"><div className={`property-image${image ? "" : " image-pending"}`}>{image && <Image src={image.public_url} alt={image.alt_text} fill sizes="(max-width: 700px) 78vw, 360px" />}{!image && <span className="pending-label">Listing images coming soon</span>}{property.featured && <span>Featured</span>}</div><div className="property-body"><p className="development-label">{property.development_name}</p><p className="location">{property.suburb}, {property.city}</p><h3>{property.title}</h3><div className="specs"><span><BedDouble size={16}/>{property.bedrooms}</span><span><Bath size={16}/>{property.bathrooms}</span><span><Users size={16}/>{property.max_guests}</span></div><p className="price">{price}</p></div></Link> }