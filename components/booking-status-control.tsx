"use client";
import { LoaderCircle } from "lucide-react";
import { useState } from "react";
import { CustomSelect } from "@/components/custom-select";
export function BookingStatusControl({bookingId,status}:{bookingId:string;status:string}){const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function update(formData:FormData){setBusy(true);const next=String(formData.get("status"));const response=await fetch(`/api/admin/bookings/${bookingId}/status`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:next})});setMessage(response.ok?"Status saved.":"Could not save status.");setBusy(false)}return <form className="status-control" action={update}><label>Update status</label><CustomSelect name="status" defaultValue={status} ariaLabel="Booking status" options={[{label:"New",value:"new"},{label:"Contacted",value:"contacted"},{label:"Confirmed",value:"confirmed"},{label:"Cancelled",value:"cancelled"},{label:"Completed",value:"completed"}]}/><button disabled={busy} className="button navy">{busy&&<LoaderCircle className="button-spinner" size={16}/>} {busy?"Saving...":"Save status"}</button>{message&&<small>{message}</small>}</form>}