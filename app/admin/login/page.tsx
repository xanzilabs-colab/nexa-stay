"use client";
import { LoaderCircle } from "lucide-react";
import { FormEvent, useState } from "react";
export default function Login(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});if(response.ok)location.href="/admin";else{const result=await response.json();setError(result.error??"Unable to sign in.");setBusy(false)}}return <main className="login"><form onSubmit={submit}><p className="eyebrow">NEXASTAY ADMIN</p><h1>Sign in</h1><label>Username<input name="username" autoComplete="username" required /></label><label>Password<input name="password" type="password" autoComplete="current-password" required /></label>{error&&<p className="form-error">{error}</p>}<button disabled={busy} className="button navy">{busy&&<LoaderCircle className="button-spinner" size={17}/>} {busy?"Signing in...":"Sign in"}</button></form></main>}